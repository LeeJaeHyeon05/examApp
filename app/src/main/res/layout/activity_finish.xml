<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">


    <TextView
        android:id="@+id/finishDialogText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text=""
        android:textSize="25sp"
        android:textColor="@color/black"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginBottom="30dp"/>
    
    <Button
        android:id="@+id/finishDialogButton"
        android:layout_width="180dp"
        android:layout_height="50dp"
        app:layout_constraintTop_toBottomOf="@+id/finishDialogText"
        app:layout_constraintStart_toStartOf="@+id/finishDialogText"
        app:layout_constraintEnd_toEndOf="@+id/finishDialogText"
        android:text="@string/checkText"
        android:background="@drawable/finish_dia_round"
        android:textSize="30sp"/>

    <View
        app:layout_constraintTop_toBottomOf="@+id/finishDialogButton"
        android:layout_width="match_parent"
        android:layout_height="20dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>